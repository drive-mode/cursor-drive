# Scripts

| Script | Purpose |
|--------|---------|
| `launch-serve-web.mjs` | Launch/serve web for E2E (browser tests) |
| `serve-web-dev.ps1` | Serve web dev server for E2E |
| `test-browser.ps1` | Run browser (Playwright) tests |
