# Webview assets

HTML templates for Drive webviews. In development (Extension Development Host), edits here hot-reload without restarting.

- **drive-sidebar.html** — Drive panel in the Activity Bar

Placeholders: `{{CSP_NONCE}}`, `{{CSP_SOURCE}}` are replaced at runtime.
