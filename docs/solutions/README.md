# Solutions

Documented solutions to non-trivial problems. Knowledge compounds here.

## Purpose

When we fix a bug or implement something non-obvious, we capture the problem, context, and solution so future work can benefit. No external APIs — pure documentation.

## Naming

`<kebab-topic>-<short-hash>.md` — e.g. `windows-path-tests-a1b2c3d.md`

## Template

See `.cursor/skills/compound-workflow/SKILL.md` for the full template. Invoke with "document this solution" or "capture what we learned".
